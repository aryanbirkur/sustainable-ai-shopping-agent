# Milestone 2 dependencies only — more will be added as later milestones need them.
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
pytest>=8.0

# Milestone 3 dependencies (Sustainability Intelligence Engine)
scikit-learn>=1.4
joblib>=1.3

# Milestone 4 dependencies (Embeddings & Semantic Search)
sentence-transformers>=3.0
chromadb>=0.5
transformers
torch
