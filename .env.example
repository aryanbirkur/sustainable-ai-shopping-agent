# Copy this file to .env and fill in real values. Never commit .env itself.

# Optional — only needed once Milestone 10 (LLM intent extraction) is enabled.
# ANTHROPIC_API_KEY=your_key_here

# Optional — used from Milestone 2 onward if you want the DB-backed path
# instead of flat CSVs in data/processed/. Not required for this milestone.
# DATABASE_URL=postgresql://user:password@localhost:5432/sustainable_ai_shopping
